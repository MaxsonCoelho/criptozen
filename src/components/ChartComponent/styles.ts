import { StyleSheet } from "react-native";

export const stylesCollections = () => {
    return StyleSheet.create({
        container: {
            color: '#FFFFFF', 
            fontSize: 18,
            marginBottom: 10
          },
    });
  };
  